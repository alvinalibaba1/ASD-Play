import SwiftUI

enum Destination: Hashable {
    case intro
    case credit
    case menu
    case sensorySettings
    case jigsawPuzzle
    case matchingPuzzle
    case sortingPuzzle
    case tracingPuzzle
    
}
